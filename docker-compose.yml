version: '3.8'

services:
  probation-backend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: probation_backend_container
    ports:
      - "3000:3000"
    env_file:
      - functions/.env
    restart: always
